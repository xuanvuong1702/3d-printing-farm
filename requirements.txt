fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
httpx>=0.27,<1.0
python-multipart>=0.0.9,<1.0

# Kênh WebSocket real-time (E2-1, D-002 phần 2 / Q-001) - KHÁC hẳn httpx
# thuần ở trên (D-002 phần 1, dùng cho lệnh HTTP rời rạc). aiohttp/
# async-timeout là ràng buộc transitive của chính moonraker-api
# (Requires-Dist: aiohttp~=3.8, async-timeout~=4.0) - ghim tường minh ở
# đây thay vì để pip tự giải quyết ngầm, cho rõ ràng khi đọc file này.
moonraker-api>=4.0,<5.0
aiohttp~=3.8
async-timeout~=4.0
